# 下一步执行顺序

当前仓库已经完成“阶段0仓库组装”和“阶段1最小领域引擎”的人工基线，并提供可运行的 P00—A2-11 逻辑骨架。

Codex 下一步不应重新创建项目，而应依次执行：

1. **权威输入复核**：运行 `pnpm verify:inputs`，确认六册文档和 UI 参考包未被替换。
2. **领域引擎加固**：完善递归事件、延迟响应队列、事务回滚、存档迁移与事件重放。
3. **iOS中性复刻**：读取 `references/ui/.../apps/ios_system/`，重做锁屏、密码键盘、主屏和系统弹层；保留当前可玩逻辑。
4. **微信中性复刻**：先用中性消息复刻会话列表、单聊、文件传输助手；Playwright比对通过后再恢复当前剧情内容。
5. **照片与Safari复刻**：完成图库、照片信息、Safari搜索、404和知识页的真实组件密度。
6. **内容资产替换**：以 AI 写实母图替换 `public/media/case-001/placeholders/` 中的两个临时 SVG，并补齐媒体 Manifest QA。
7. **完整切片QA**：运行 `pnpm qa`，生成 BUILD_REPORT、VISUAL_DIFF_REPORT 和 KNOWN_GAPS。

## 禁止

- 不得删除当前事件与内容合同后重新写一套页面状态。
- 不得把参考截图直接作为页面背景。
- 不得在视觉复刻前改写正式聊天、帖子和谜题。
- 不得让随机生成内容进入正式内容包。

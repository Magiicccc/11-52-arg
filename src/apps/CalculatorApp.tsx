import { useState } from "react";
import { AppChrome } from "@/shell/AppChrome";
export function CalculatorApp(){ const [value,setValue]=useState("0"); const [stored,setStored]=useState<number|null>(null); const [op,setOp]=useState<string|null>(null); const press=(key:string)=>{if(/\d/.test(key))setValue(v=>v==="0"?key:(v+key).slice(0,12));else if(key==="C"){setValue("0");setStored(null);setOp(null)}else if(key==="="){if(stored!==null&&op){const n=Number(value);const r=op==="+"?stored+n:op==="−"?stored-n:op==="×"?stored*n:stored/n;setValue(String(Number.isFinite(r)?r:0).slice(0,12));setStored(null);setOp(null)}}else{setStored(Number(value));setOp(key);setValue("0")}}; return <AppChrome title="计算器"><div className="calculator"><output>{value}</output><div>{["C","±","%","÷","7","8","9","×","4","5","6","−","1","2","3","+","0",".","="].map(k=><button data-testid={k==="1"?"app-effective-action":undefined} key={k} onClick={()=>press(k)}>{k}</button>)}</div></div></AppChrome>; }
